node_modules
.next
out
*.log
.DS_Store
.env
.env.local

# Local DB + backups + agent state — never committed (sensitive lead data)
data/*.db
data/*.db-*
data/*.json
data/backups/
!data/.gitkeep

# Don't push the day6_track_a/ scratch state either
data/day6_track_a/
data/round8_state/

.vscode
